class CartModal{
final String img;
final String productName;
final String productColor;
final String amount;
final String cartNo;

  CartModal(this.img, this.productName, this.productColor, this.amount, this.cartNo);
}