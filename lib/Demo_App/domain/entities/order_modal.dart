class OrderModal{
final String img;
final String productName;
final String productColor;
final String amount;
final String date;
final String status;

  OrderModal(this.img, this.productName, this.productColor, this.amount, this.date, this.status);
}